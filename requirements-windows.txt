# 100% pure Python wheels — zero compilation on any Windows/Python version.
# Tested on Python 3.9 through 3.14.
# No pydantic, no pydantic-core, no Rust, no C compiler needed.
flask==3.0.3
flask-cors==4.0.1
waitress==3.0.0
python-dotenv==1.0.1
httpx==0.27.0